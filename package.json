{
  "name": "repo-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Ferramenta local para instalar e rodar scripts de múltiplos repositórios irmãos no mesmo diretório, com seleção interativa ou por CLI e suporte a NVM (Windows e Unix).",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install": "node scripts/repo-workspace.mjs install",
    "dev": "node scripts/repo-workspace.mjs dev",
    "repos:install": "node scripts/repo-workspace.mjs install",
    "repos:dev": "node scripts/repo-workspace.mjs dev"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "prompts": "^2.4.2"
  }
}
