{
  "name": "repo-workspace",
  "version": "1.5.0",
  "private": true,
  "description": "Centralizador local para instalar dependências, executar desenvolvimento e testes, preparar ambientes e trocar branches em múltiplos repositórios irmãos.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install": "node scripts/repo-workspace.mjs install",
    "dev": "node scripts/repo-workspace.mjs dev",
    "test": "node scripts/repo-workspace.mjs test",
    "test:self": "node --test tests/args.test.mjs tests/config.test.mjs tests/git.test.mjs tests/root.test.mjs tests/run.test.mjs tests/select.test.mjs tests/smoke.test.mjs tests/test-command.test.mjs tests/workspace.test.mjs",
    "setup": "node scripts/repo-workspace.mjs setup",
    "open": "node scripts/repo-workspace.mjs open",
    "switch": "node scripts/repo-workspace.mjs switch",
    "repos:install": "node scripts/repo-workspace.mjs install",
    "repos:dev": "node scripts/repo-workspace.mjs dev",
    "repos:test": "node scripts/repo-workspace.mjs test",
    "repos:setup": "node scripts/repo-workspace.mjs setup",
    "repos:open": "node scripts/repo-workspace.mjs open"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "prompts": "^2.4.2"
  }
}
